import flet as ft


def inventory_view_content(page: ft.Page):
    return ft.Column([ft.Text('Inventory Screen Ready', size=40, weight='bold', color='#4A4440'), ft.Text('This is where the inventory management happens.')])
